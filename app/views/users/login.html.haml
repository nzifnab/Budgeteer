%h1.pageTitle Please Login
- form_for :user, :url => {:action => 'enter'}, :html => {:method => :put} do |f|
  = f.label :username, "Username"
  = f.text_field :username
  %br
  = f.label :password, "Password"
  = f.password_field :password
  %br
  = f.submit "Login"
